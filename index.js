/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import HomeMeetings from './src/screens/homeSlots'

AppRegistry.registerComponent(appName, () => HomeMeetings);
